module.exports = {

    // header size = 14 bytes
    // DIV header = 40
    // extra bit masks = ?
    // color table = ?
    // gap 1 = ?

    // probably (file size - image size)? 
    PIXEL_OFFSET: 1078,

    BITS_PER_PIXEL_OFFSET: 8,
    FILE_SIZE_OFFSET: 11078
};

// image size 10,000  ()
// height = width = 64